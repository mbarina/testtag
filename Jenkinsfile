pipeline {
    agent any
    stages {
        stage("test") {
            steps {
              script {
                echo env.getEvironment()
              }
              echo "Done"
            }
        }
    }
}
