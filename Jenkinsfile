pipeline {
    agent any
    stages {
        stage("test") {
        script {
            echo env.getEvironment()
        }
            steps {
                echo "Done"
            }
        }
    }
}
