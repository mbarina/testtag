pipeline {
    agent any
    stages {
        stage("test") {
            steps {
              script {
                echo env.getEnvironment()
              }
              echo "Done"
            }
        }
    }
}
